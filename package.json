{
  "name": "hyperframes-app",
  "private": true,
  "workspaces": [
    "packages/pipeline",
    "packages/shell"
  ],
  "scripts": {
    "dev": "npm run build --workspace=packages/pipeline && concurrently -n pipeline,shell -c blue,green \"npm run dev --workspace=packages/pipeline\" \"npm run dev --workspace=packages/shell\"",
    "build": "npm run build --workspace=packages/pipeline && npm run build --workspace=packages/shell"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
